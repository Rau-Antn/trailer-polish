План исправления:

1. В `public/css/style.css` убрать ограничение, из-за которого Lottie визуально остаётся маленькой/слева: контейнер `.search-lottie` и вложенный `svg` будут принудительно растянуты на `width: 100%; height: 100%` раскрытой кнопки.
2. Для hover-состояния сделать анимацию именно фоном всего pill-окна поиска: `.search-lottie` будет покрывать всю ширину открытого `.search-toggle`, без отдельной узкой зоны и без участия input.
3. Убрать `display:flex`-центрирование у Lottie-контейнера, потому что оно может оставлять сам SVG в исходной маленькой области; заменить на абсолютное покрытие всей площади.
4. В открытом по клику состоянии оставить текущее поведение: Lottie скрывается, показывается чистое поле ввода.
5. Проверить в текущем размере превью 807×494: наведение = анимация на всю раскрытую ширину окна поиска, клик = анимация пропадает и работает ввод.